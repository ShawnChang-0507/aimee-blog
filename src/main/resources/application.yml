server:
  servlet:
    session:
      timeout: 1800
  port: 8080
spring:
  datasource:
    username: root
    password: pythoner
    url: jdbc:mysql://127.0.0.1:3306/aimee_blog?useUnicode=true&characterEncoding=utf8&useSSL=true
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    cache: false
    mode: HTML5
  redis:
    host: 127.0.0.1
    port: 6379
    jedis:
      pool:
        max-idle: 8
        min-idle: 0
        max-active: 8
        max-wait: -1
    timeout: 0
mybatis:
  mapper-locations: classpath*:com/fullexception/mapping/*.xml
  type-aliases-package: com.fullexception.entity
logging:
  level:
    com.fullexception.mapper: DEBUG